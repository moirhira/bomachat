global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "boma_irc"

    metrics_path: '/metrics'

    static_configs:
      - targets:
        - "bomachat:9101"

  - job_name: "node_exporter"

    static_configs:
      - targets:
        - "node_exporter:9100"
